'''
A virtual enviroment is a tool used to isolate specific
python  enviroment o a single machine, allowing you to
workk on multiple projects with defferent dependencies 
and packges with conflicts.This  can be especially useful 
when working on projects that have conflicting package
version or packages that are not compatible with each other;

'''
import pandas as pd

print(pd.__version__)